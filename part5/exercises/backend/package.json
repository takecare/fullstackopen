{
  "name": "blog",
  "version": "0.0.1",
  "description": "part 4",
  "main": "index.js",
  "scripts": {
    "start": "NODE_ENV=production; node --inspect index.js",
    "dev": "NODE_ENV=development; nodemon index.js",
    "lint": "eslint .",
    "test": "NODE_ENV=test; jest --verbose --runInBand",
    "test:debug": "NODE_ENV=test; jest --verbose --runInBand --detectOpenHandles"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "eslint": "^7.0.0",
    "jest": "^26.0.1",
    "nodemon": "^2.0.4",
    "supertest": "^4.0.2"
  },
  "dependencies": {
    "bcrypt": "^4.0.1",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "express-async-errors": "^3.1.1",
    "jsonwebtoken": "^8.5.1",
    "mongoose": "^5.9.14",
    "mongoose-unique-validator": "^2.0.3"
  }
}
